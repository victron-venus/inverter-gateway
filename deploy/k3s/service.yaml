apiVersion: v1
kind: Service
metadata:
  name: inverter-gateway
spec:
  type: NodePort
  selector:
    app: inverter-gateway
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30150
